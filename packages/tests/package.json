{
  "name": "tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test --env-file=../../.env.test --coverage --timeout 120000"
  },
  "dependencies": {
    "api": "workspace:*",
    "services": "workspace:*",
    "db": "workspace:*",
    "config": "workspace:*",
    "reporting": "workspace:*",
    "exceljs": "^4.4.0",
    "mongodb-memory-server": "^10.0.0"
  }
}
