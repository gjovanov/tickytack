{
  "name": "e2e",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun run seed && SKIP_SEED=1 PATH=$HOME/.local/node/bin:$PATH ./node_modules/.bin/playwright test",
    "test:ui": "bun run seed && SKIP_SEED=1 PATH=$HOME/.local/node/bin:$PATH ./node_modules/.bin/playwright test --ui",
    "seed": "bun run --env-file=../../.env global-setup.ts"
  },
  "dependencies": {
    "config": "workspace:*",
    "db": "workspace:*",
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0"
  }
}
